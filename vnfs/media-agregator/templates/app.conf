{% extends "nginx.conf" %}

{% block app %}
{% if app %}
application {{app}} {
            live on;
            record off;
            {% if push_url %}
                {{push_url}}
            {% endif %}
        }
{% endif %}
{% endblock %}